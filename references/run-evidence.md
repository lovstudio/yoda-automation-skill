# Run Evidence

一次自动化配置完成的最低证据包括：

- 自动化记录存在且 `status=active`。
- `trigger_kind` 与 cron 或一次性计划相符。
- 时区明确，`next_run_at` 位于未来。
- 最新运行的状态、开始时间、结束时间和错误信息已被读取。
- 任务有明确完成门和停用动作。

历史运行失败只能说明失败发生过。必须把失败原因与当前保存后记录分开陈述，例如“此前因应用退出失败；当前计划已回读为工作日 10:00，仍待下一次真实运行验证”。
